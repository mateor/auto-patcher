# INSTRUCTION

1. build modules by "make framework core services"
   and save under "diff_tools/save/$PATCH/$ROM/stock.$DATE.$DEVICE"

2. apply $PATCH patch
   and build modules by "make framework core services"
   and save under "diff_tools/save/$PATCH/$ROM/$PATCH.$DATE.$DEVICE"

3. run "./batch.sh"

# TODO
1. need to automate file list
